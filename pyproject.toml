[project]
name = "kitchen-to-digital"
version = "0.1.0"
description = "Kafka-based kitchen IoT data platform"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/sensor-simulator",
    "packages/*",
]

[dependency-groups]
dev = [
    "pytest>=8.3,<9.0",
    "pytest-asyncio>=0.24,<1.0",
    "ruff>=0.7,<1.0",
]
